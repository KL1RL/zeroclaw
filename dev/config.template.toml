workspace_dir = "/zeroclaw-data/workspace"
config_path = "/zeroclaw-data/.zeroclaw/config.toml"
# This is the Ollama Base URL, not a secret key
api_key = "http://host.docker.internal:11434"
default_provider = "ollama"
default_model = "llama3.2"
default_temperature = 0.7

[gateway]
port = 42617
host = "[::]"
allow_public_bind = true
require_pairing = false

# Home Assistant integration
[home_assistant]
enabled = false
url = "http://homeassistant.local:8123"
disable_strict_ssl = false

# Signal channel (optional)
# [channels_config.signal]
# http_url = "http://127.0.0.1:8686"
# account = "+1234567890"
# group_id = "dm"                  # optional legacy scope filter: "dm" / specific group id / omitted
# allowed_from = ["*"]
# allowed_groups = ["group-id"]    # optional: members of these groups may message without being in allowed_from
# ignore_attachments = false
# ignore_stories = true
# mention_only = false              # optional: require explicit mentions in groups; DMs always allowed

# Cost tracking and budget enforcement configuration
# Enable to track API usage costs and enforce spending limits
[cost]
enabled = false
daily_limit_usd = 10.0
monthly_limit_usd = 100.0
warn_at_percent = 80
allow_override = false

# Per-model pricing (USD per 1M tokens)
# Uncomment and customize to override default pricing
# [cost.prices."anthropic/claude-sonnet-4-20250514"]
# input = 3.0
# output = 15.0
#
# [cost.prices."openai/gpt-4o"]
# input = 5.0
# output = 15.0
